<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.geekie.shop.shoppingmall.mapper.OrderItemArchiveMapper">

    <resultMap id="BaseResultMap" type="site.geekie.shop.shoppingmall.entity.OrderItemDO">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="batchInsert">
        INSERT INTO mall_order_item_archive (
            id, order_id, product_id, product_name, product_image,
            unit_price, quantity, total_price, created_at
        ) VALUES
        <foreach collection="items" item="item" separator=",">
            (
                #{item.id}, #{item.orderId}, #{item.productId}, #{item.productName},
                #{item.productImage}, #{item.unitPrice}, #{item.quantity},
                #{item.totalPrice}, #{item.createdAt}
            )
        </foreach>
    </insert>

    <select id="findByOrderIds" resultMap="BaseResultMap">
        SELECT id, order_id, product_id, product_name, product_image,
               unit_price, quantity, total_price, created_at
        FROM mall_order_item_archive
        WHERE order_id IN
        <foreach item="orderId" collection="orderIds" open="(" separator="," close=")">
            #{orderId}
        </foreach>
        ORDER BY order_id, id
    </select>

</mapper>
